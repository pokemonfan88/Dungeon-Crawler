# 种族系统实装前审计报告

审计范围：4种族（人族/精灵/熊人/矮人）集成到烛照幽渊 v2 现有系统

---

## 1. calcHeroStats() — ✅ 无冲突，需扩展

函数从 `S.hero` 取 `baseAtk/baseDef/maxHp/maxSanity/critChance/sanityResist/torchDrainMult/dodge` 等属性。

**实装方案**：种族选择时直接修改 `S.hero` 的属性偏移值，`calcHeroStats` 无需改动。

**需新增字段**：
```javascript
hero: {
  race: "human",           // 种族ID
  baseDodge: 0,            // 基础闪避率（矮人=-0.05）
  bloodrageActive: false,  // 血怒状态标记（熊人运行时）
  eventRewardMul: 1,       // 事件奖励倍率（精灵=1.3）
  smithCostMul: 1,         // 铁匠费用倍率（矮人=0.7）
}
```

---

## 2. stepTick() — ⚠️ 3 处需修改

### 2.1 熊人·血怒（需新增代码）
```
位置: stepTick 中 combat 段 (约 line 1143)
需求: 当 h.sanity < 30% && race==="bear" 时：
  - attack ×1.4（新增 bloodrageMul）  
  - crit +10%
  - 禁用全部技能（在 useEffect 和渲染中检查）
```

### 2.2 精灵·火把消耗（需修改）
```
位置: stepTick 中 torchDrain 计算 (约 line 1109)
当前: const torchDrain = (...) * h.torchDrainMult * stats.torchBlessingMul;
需求: 精灵 h.torchDrainMult = 0.85
方案: 种族选择时直接设置 h.torchDrainMult = 0.85，无需改 stepTick ✅
```

### 2.3 树人光合残存（本次不实装，跳过）

---

## 3. 技能系统 — ⚠️ 需要架构调整

### 3.1 种族独有技能
当前 SKILLS 数组是全局的5个通用技能。种族技能需要：

| 种族 | 技能ID | 效果 |
|------|--------|------|
| 人类 | inspire | 清除debuff，冷却60s |
| 精灵 | forestStep | 触发探索事件，消耗10神智，冷却120s |
| 熊人 | roar | 怪物下回合不攻击，消耗15神智，冷却45s |
| 矮人 | mountainHeart | 8秒伤害减免50%，消耗20神智，冷却90s |

### 3.2 技能ID冲突
种族技能ID不与通用技能冲突 ✅（无同名技能）

### 3.3 技能按钮HTML
当前5个技能按钮硬编码在HTML中。需改为动态渲染（根据当前种族显示对应技能）。

**当前问题**：`skillBerserk` 等按钮ID是硬编码的CSS id。种族技能按钮需要在 `renderExpedition` 中动态追加。

**方案**：将技能按钮从硬编码HTML改为 `skillBar` 的 `innerHTML` 动态生成，如消耗品栏的做法一致。

### 3.4 熊人血怒禁用技能
需要两处检查：
1. `useSkill()` — 开头加 `if(S.hero.race==="bear" && S.hero.sanity < S.hero.maxSanity*0.3) return;`
2. `renderExpedition` 技能按钮 — 加上 disabled 条件

---

## 4. endExpedition() — ⚠️ 1 处需修改

### 4.1 人类·韧性
```
位置: endExpedition 约 line 1016
当前: S.hero.sanity = Math.max(0, S.hero.sanity - sanityPenalty);
需求: 人类先恢复25%再扣惩罚
      if(S.hero.race==="human") S.hero.sanity = Math.min(effMaxSanity, S.hero.sanity + effMaxSanity*0.25);
      S.hero.sanity = Math.max(0, S.hero.sanity - sanityPenalty);
```

---

## 5. 铁匠铺 — ⚠️ 2 处需修改

### 5.1 矮人·鉴定费用 -30%
```
identifyItem() line 2081:
当前: const cost = Math.round((item.sellPrice||10)*0.4);
需求: const discount = S.hero.race==="dwarf"?0.7:1;
      const cost = Math.round((item.sellPrice||10)*0.4*discount);
```

### 5.2 矮人·打造费用 -30% + 材料需求-1
```
craftItem() line 2103:
当前: S.gold -= recipe.cost;
需求: const cost = S.hero.race==="dwarf"?Math.round(recipe.cost*0.7):recipe.cost;
      S.gold -= cost;
```

材料需求减1在 `craftItem` 的 `recipe.materials.forEach` 中调整 qty。

**风险**：材料需求最低为1，不能减到0。需 `Math.max(1, m.qty-(S.hero.race==="dwarf"?1:0))`。

---

## 6. 探索事件 — ⚠️ 1 处需修改

### 6.1 精灵·事件奖励 ×1.3
```
resolveEvent() 中 applyEventEffect 调用
方案: 在 applyEventEffect 的 gold/torch/sanity 效果中乘以 multiplier
修改点: applyEventEffect 函数开头加 multiplier = S.hero.race==="elf"?1.3:1
```

### 6.2 精灵·族技"林间步"
```
useSkill("forestStep") → 消耗10神智 → ex._forceEvent = true
stepTick 中事件触发前检查 ex._forceEvent �� 强制触发事件
```

---

## 7. 死亡/复活系统 — ⚠️ 边缘情况检查

### 7.1 熊人血怒 + 死亡
低神智时攻击×1.4 但无法用技能（包含不能自我治疗）。存在"越战越勇但越来越危险"的正反馈循环。设计上**有意为之**，不是bug。

### 7.2 矮人·山之心 + 闪避惩罚
矮人闪避 -5%，但山之心可减伤50%。这两个机制互相补偿。✅

### 7.3 复活检查
`stepTick` 中复活逻辑取 `S.equipment.relic.stats.revive`，与种族无关。✅

---

## 8. 远征渲染 — ⚠️ 2 处需修改

### 8.1 种族标识
需要在远征视图显示当前种族名称和图标。在 `expLayerName` 旁边添加。

### 8.2 技能按钮动态化
见 §3.3，需将硬编码按钮改为动态渲染。

---

## 9. 数值平衡 — ⚠️ 需要关注

### 9.1 种族战斗力对比（基准人族 = 100%）

| 维度 | 人类 | 精灵 | 熊人 | 矮人 |
|------|------|------|------|------|
| 攻击力 | 100% | 75% | 150% | 125% |
| 防御力 | 100% | 100% | 150% | 300% |
| 血量 | 100% | 100% | 160% | 120% |
| 神智 | 100% | 120% | 75% | 100% |
| 闪避 | 基准 | 基准 | 基准 | -5% |
| 综合战斗力 | 中 | 中低(探索特化) | 极高(神智风险) | 高(防御特化) |

### 9.2 风险评估

| 风险 | 严重度 | 说明 |
|------|--------|------|
| 熊人过强 | 🔴 高 | 1.5×atk + 血怒1.4× = 2.1×atk 峰值。需降低基础atk偏移为+3 |
| 精灵过弱 | 🟡 中 | atk -2 让精灵缺乏战斗力。事件奖励×1.3难以量化补偿。建议改为 atk -1 |
| 矮人打造折扣 | 🟢 低 | -30% 费用合理，主要在后期省钱 |
| 人类无特色 | 🟢 低 | 韧性是隐性强力被动，非炫耀性但实用 |

### 9.3 建议调整

| 种族 | 原值 | 建议值 | 原因 |
|------|------|--------|------|
| 熊人 atk | +4 | **+3** | 血怒1.4×加成后等效 atk+8.2，已足够强力 |
| 精灵 atk | -2 | **-1** | 事件奖励×1.3难以补足-25%攻击损失 |
| 矮人 maxHp | +10 | **+5** | 防御+4 已经很强，血量不需要太多 |

---

## 10. 存档兼容 — ✅ 可处理

新增字段需在 `load()` 中做兼容：
```javascript
if(!merged.hero.race) merged.hero.race = "human";
if(merged.hero.baseDodge===undefined) merged.hero.baseDodge = 0;
```

种族相关的派生属性（如 torchDrainMult）在选择种族时动态设置，不需要存档持久化。

---

## 11. UI 改动清单

| 页面 | 改动 |
|------|------|
| 整备 | 顶部显示当前种族图标+名称 |
| 深渊之门 | 出征前可选择种族（已解锁的）|
| 远征视图 | 标题栏显示种族，技能栏动态渲染 |
| 设置 | 显示已解锁种族列表 |
| 新增 | 种族选择面板（营地新标签或深渊之门内）|

---

## 总结

| 类别 | 数量 |
|------|------|
| ✅ 无冲突可直接集成 | 4 处 |
| ⚠️ 需修改现有代码 | 8 处 |
| 🔴 高风险需调整数值 | 2 处 |
| 🆕 需新增代码 | 4 个函数 + 1 个UI面板 |

**结论**：可以安全实装，但需要先处理数值调整（熊人atk、精灵atk）和技能系统重构（动态化技能按钮）。
